function solve(input){
     let index = 0;
     let words = input[index];
     while(words !== 'Stop'){
           console.log(words);
           index++;
           words = input[index];
        }
   }